<Window x:Class="WPF_ListBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="MainWindow" Height="200" Width="200">
    <Grid>
    <!--<ListBox>
            <ListBoxItem>
                <ListBoxItem.Content>
                    <WrapPanel>
                        <CheckBox VerticalAlignment="Center"></CheckBox>
                        <Label>Yellow</Label>
                    </WrapPanel>
                </ListBoxItem.Content>
            </ListBoxItem>
            <ListBoxItem>
                <ListBoxItem.Content>
                    <WrapPanel>
                        <CheckBox VerticalAlignment="Center"></CheckBox>
                        <Label>Green</Label>
                    </WrapPanel>
                </ListBoxItem.Content>
            </ListBoxItem>
            <ListBoxItem>
                <ListBoxItem.Content>
                    <WrapPanel>
                        <CheckBox VerticalAlignment="Center"></CheckBox>
                        <Label>Blue</Label>
                    </WrapPanel>
                </ListBoxItem.Content>
            </ListBoxItem>
        </ListBox>-->
        <ListBox ItemsSource="{Binding Path=ColorList}"></ListBox>
    </Grid>
</Window>
